## Constructing a Valid Request

TODO: should not be markdown, instead has to be interactive request (user can click send, and returns results)
or should it be hardcoded

Reference: [Mixpanel query tester](https://developer.mixpanel.com/reference/segmentation-query)
