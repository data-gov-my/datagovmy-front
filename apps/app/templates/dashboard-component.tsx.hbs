import AgencyBadge from "@components/Badge/agency";
import { Hero, Section } from "@components/index";
import { useTranslation } from "@hooks/useTranslation";
import { FunctionComponent } from "react";
import Container from "@components/Container";

/**
 * {{titleCase dashboard_name}} Dashboard
 * @overview Status: In-development
 */

interface {{pascalCase dashboard_name}}Props {}

const {{pascalCase dashboard_name}}: FunctionComponent<{{pascalCase dashboard_name}}Props> = ({}) => {
  const { t, i18n } = useTranslation(["dashboard-{{dashCase dashboard_name}}", "common"]);

  return (
    <>
      <Hero
        background="gray"
        category={[t("common:categories.{{dashboard_category}}", "text-black"]}
        header={[t("header")]}
        description={[t("description")]}
        agencyBadge={
          <AgencyBadge
            agency="{{titleCase agency_name}}"
            link="{{agency_link}}"
          />
        }
      />
      {/* Rest of page goes here */}
      <Container className="min-h-screen">
        {{> sectionPartial}}
      </Container>
    </>
  );
};

export default {{pascalCase dashboard_name}};
